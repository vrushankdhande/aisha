# Aisha — Platinum Insurance Voice Assistant
# Install with: pip install -r requirements.txt
 
streamlit>=1.35.0
sounddevice>=0.4.6
numpy>=1.26.0
gtts>=2.5.1
google-genai>=1.0.0
 
# ffmpeg is also required for audio conversion (not a pip package)
# Install separately:
#   Windows : winget install ffmpeg   OR   choco install ffmpeg
#   macOS   : brew install ffmpeg
#   Ubuntu  : sudo apt install ffmpeg
#
# winsound is built into Python on Windows — no install needed.
# io, wave, time, tempfile, os are Python standard library modules.
 